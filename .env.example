PORT=3000
# Your MongoDB connection string (MongoDB Atlas: Connect > Drivers).
# The app uses the database named after ".net/" and creates its own "trivia_" collections there.
DATABASE_URL="mongodb+srv://<username>:<password>@<cluster-host>/<database-name>?appName=<app-name>"
TRUST_PROXY=false
